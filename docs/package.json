{
  "name": "nuxt-feathers-zod-docs",
  "type": "module",
  "private": true,
  "packageManager": "bun@1.3.6",
  "scripts": {
    "dev": "bunx vitepress dev . --host",
    "build": "bunx vitepress build .",
    "preview": "bunx vitepress preview .",
    "docs:dev": "bun run dev",
    "docs:build": "bun run build",
    "docs:preview": "bun run preview"
  },
  "devDependencies": {
    "vitepress": "^1.3.1"
  }
}
